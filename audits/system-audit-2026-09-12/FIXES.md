# Исправления по системному аудиту — 12 сентября 2026

Исправления внесены в текущее рабочее дерево поверх существовавших незакоммиченных правок. Коммиты не создавались: до начала работы дерево уже содержало пользовательские изменения.

## Закрытые направления

| Направление | Исправление |
|---|---|
| Удаление аккаунта | Очередь удаления закрыта от клиентских write-операций; Auth ID канонически сверяется с `users.auth_id`; отмена атомарна; worker использует lease/retry; удаление владельца каталога совместимо с FK; дочерние тренировочные сессии привязаны к владельцу плана. |
| История, импорт и erasure | Локальный vault закреплён за владельцем; UUID канонизируются; чувствительные поля снова шифруются при импорте; local-only restore remap-ит владельца; HealthKit не трактует пустой ответ как удаление; tombstone/outbox и erasure fences стали безопасными. |
| Уведомления и Watch | Server push unregister доставляется до sign-out; erasure отменяет системные уведомления; действия Watch сохраняют owner, время, timezone, день и supplement ID. |
| Питание и тренировки | Сохраняется выбранная дата еды; порция пересчитывает все макросы; AI-кандидаты остаются редактируемыми; OCR переводит serving в 100 г; календарь учитывает первый день недели; длительность тренировки задаётся явно. |
| БАДы, анализы, эксперименты, Wellness | Расписания и adherence считаются по запланированным слотам; доступны исправление/undo; marker анализов можно исправить/удалить с tombstone sync; refresh не затирает pending local edits; фазы эксперимента вычисляются актуально; reopen сохраняет notes/adherence; добавлены PSS-4 и корректный факт показа ресурсов. |
| Планы и рекомендации | Сервер создаёт исполнимые тренировки с упражнениями; adaptive adjustment меняет будущие сессии атомарно; направления трендов исправлены; AI-прогноз нормализует диапазон/зону и откатывается к deterministic output без истории. |
| Первый запуск | Фото еды стало необязательным; UI-тест проходит через «Пропустить пока» без test-only completion; purpose string геопозиции дополнен русским текстом; контент онбординга прокручивается безопасно. |

Новые миграции: `20260912000001_account_deletion_lifecycle_hardening.sql` и `20260912000002_training_plans_and_measurement_tombstones.sql`.

## Проверка

- `deno fmt --check`, `deno lint`, `deno check`: успешно.
- `deno test -A supabase/functions/tests`: **290 passed, 0 failed**.
- Чистый локальный Supabase reset, SQL integrity checks и `scripts/run_supabase_edge_e2e.sh`: успешно; применены 25 миграций.
- Полный iOS unit + widgets: **837 passed, 5 skipped, 0 failed** из 842.
- Отдельный UI onboarding через настоящий skip: **1 passed, 0 failed**.
- Полный Watch test target: **7 passed, 0 failed**.

Проверки не заменяют физический iPhone/Watch, hosted Supabase, реальные APNs/AI и пользовательский пилот. Эти внешние контуры по-прежнему требуют отдельной предрелизной проверки.
